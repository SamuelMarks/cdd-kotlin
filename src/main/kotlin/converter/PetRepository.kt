package io.offscale.example.repository

import io.offscale.example.models.Pet

class PetRepository{
	private val pets : MutableList<Pet> = mutableListOf()

	fun showPetById() : Pet {

	}
 
    /** Here should be implemented the functions for the repository:
    *
    * Example:
    *  fun getAllPets(): List<Pet> = pets
    *
    *  fun getPetById(Id: Int): Pet? = pets.find { it.id == id }
    *
    *  fun addPet(pet: Pet) {
    *        pets.add(pet)
    *    }
    */
    
}

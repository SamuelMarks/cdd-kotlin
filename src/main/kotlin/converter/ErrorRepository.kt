package io.offscale.example.repository

import io.offscale.example.models.Error

class ErrorRepository{
	private val errors : MutableList<Error> = mutableListOf()

	fun listPets() : Error {

	}

	fun createPets() : Error {

	}

	fun showPetById() : Error {

	}
 
    /** Here should be implemented the functions for the repository:
    *
    * Example:
    *  fun getAllErrors(): List<Error> = errors
    *
    *  fun getErrorById(Id: Int): Error? = errors.find { it.id == id }
    *
    *  fun addError(error: Error) {
    *        errors.add(error)
    *    }
    */
    
}
